# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'E health App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for E health App

  target 'E health AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'E health AppUITests' do
    # Pods for testing
  end
  
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'ChameleonFramework'
  pod 'Firebase/Analytics'
  pod 'Firebase/Messaging'
  
  # webservice
  pod 'Alamofire'
  pod 'SwiftyJSON'
  
  # for image
  pod 'SDWebImage'

  # for chat text view
  pod 'GrowingTextView'
  
  # for calendar
  pod 'FSCalendar'
  
  pod 'SwiftGifOrigin'
  
  pod 'NewYorkAlert'


end
